class PlannerAgent:

    def create_plan(self, topic):

        return [
            f"Research background of {topic}",
            f"Find latest developments about {topic}",
            f"Collect statistics for {topic}",
            f"Generate report for {topic}"
        ]
